using System;
using UnityEngine;
using MonoBehaviour = UnityEngine.MonoBehaviour;

public class AppsFlyerScript : MonoBehaviour
{
    private void Start()
    {
        Debug.Log("AppsFlyerScript");
    }
}